[coding]
# --- Fresh Hetzner box (root only on init) ---------------------------------
# Step 1 (once): create the dev user, connecting as root:
#   scripts/bootstrap.sh --limit box1
# Step 2 (now and always): provision, connecting as the dev user:
#   scripts/provision.sh --limit box1
# box1 ansible_host=1.2.3.4

# --- Any host where the dev user already exists ----------------------------
# box2 ansible_host=100.x.y.z

# --- Skip tailscale on a specific host -------------------------------------
# box3 ansible_host=10.0.0.5 tailscale_enabled=false
